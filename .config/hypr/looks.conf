$colorMain = rgba($skyAlphabb)

general {
    gaps_in = 1
    gaps_out = 0, 2, 0, 2

    border_size = 2

    col.active_border = $colorMain
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

group {
    col.border_active = $colorMain
    col.border_inactive = rgba(595959aa)
    groupbar {
        indicator_height = 2
        font_family = JetBrainsMono NF
        font_size = 12
        col.active = $colorMain
        col.inactive = rgba(595959aa)
    }
}

decoration {
    rounding = 1

    active_opacity = 1.0
    inactive_opacity = 0.95

    shadow {
        enabled = true
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

animations {
    enabled = no

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# smart gaps
workspace = w[tv1], gapsout:0, gapsin:0
workspace = f[1], gapsout:0, gapsin:0
windowrule = border_size 0, match:float 0, match:workspace w[tv1]
windowrule = rounding 0, match:float 0, match:workspace w[tv1]
windowrule = border_size 0, match:float 0, match:workspace f[1]
windowrule = rounding 0, match:float 0, match:workspace f[1]

dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
    force_split = 2
}

misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}

# this is from omarchy and i do not know what it does
# Ignore maximize requests from apps. You'll probably like this.
# windowrule = suppress_event maximize, match:class .*
# Fix some dragging issues with XWayland
# windowrulev2 = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

# i do not use animations and flameshot works fine
# Enhance flameshot behaviour
# windowrulev2 = noanim, class:(flameshot) 
# windowrulev2 = float, class:(flameshot) 

# switched to waywall
#minecraft
# $minecraftMatch = ^Minecraft.*
# $ninjabrainMatch = ^ninjabrainbot.*
# windowrulev2 = norounding, initialClass:$minecraftMatch
# windowrulev2 = noanim, initialClass:$minecraftMatch
# windowrulev2 = bordersize 0, initialClass:$minecraftMatch
# windowrulev2 = rounding 6, initialClass:$ninjabrainMatch
# windowrulev2 = noanim, initialClass:$ninjabrainMatch
# windowrulev2 = bordersize 0, initialClass:$ninjabrainMatch

# Floating windows
windowrule = match:tag floating-window, float on
windowrule = match:tag floating-window, center on
windowrule = match:tag floating-window, size 800 600

# omarchy-tui-install
windowrule = match:class TUI.float, tag +floating-window
$regexClass = (xdg-desktop-portal-gtk|sublime_text|DesktopEditors|org.gnome.Nautilus)
$regexTitle = ^(Open.*Files?|Open [F|f]older.*|Save.*Files?|Save.*As|Save|All Files)
windowrule = match:class $regexClass, match:title $regexTitle, tag +floating-window, 
# windowrule = float, class:org.gnome.Calculator
